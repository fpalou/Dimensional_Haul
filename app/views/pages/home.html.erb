<!DOCTYPE html>
<html>
  <head>
    <title>Dimensional Haul</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="home-page">
    <%= render "shared/banner" %>
    <div class="home-info">
      <div class="row">
        <div class="col-12 ">
          <h3>Discover all possibilities</h3>
        </div>
  <% i = 0 %>
        <% @dimensions.each do |dimension| %>
          <div class="col-12 col-md-6 col-lg-4">
            <div class="card home-card mt-3 mr-3">
              <% if dimension.photo.key %>
                <%= cl_image_tag dimension.photo.key, height: 400, width: 400, crop: :fill, class: "card-img-top" %>
              <% end %>
              <div class="card-body">
                <h5 class="card-title"><%= dimension.title %></h5>
                <p class="card-text"><%= dimension.description %></p>
                <%= link_to "Show", dimension_path(dimension), class: "btn btn-primary" %>
              </div>
            </div>
          </div>
          <% i += 1 %>
          <%break if i > 2 %>
        <% end %>
      </div>
      <%# <div class="home-info">
        <h3>Here goes some fancy page info</h3>
      </div> %>
    </div>
      <div class="footer mt-auto py-3">
        <div class="footer-links">
          <a href="https://github.com/fpalou/Dimensional_Haul"><i class="fab fa-github"></i></a>
          <a href="https://dimensional-haul-623e2c8f5718.herokuapp.com/"><i class="fab fa-k"></i></a>
          <a href="https://www.figma.com/file/6n1gVK8bOPRqLdhaz4mB2D"><i class="fa-solid fa-pen-fancy"></i></a>
        </div>
        <div class="footer-copyright">
          <p>Infinite realities, infinite possibilities 🚀 by the Dimensional Haul</p>
        </div>
      </div>
    </body>
  </html>
