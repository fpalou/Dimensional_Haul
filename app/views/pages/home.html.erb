<!DOCTYPE html>
<html>
  <head>
    <title>DimensionalHaul</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render "shared/banner" %>
    <div class="row">
      <div class="col-12">
        <h1>Home</h1>
        <p>Here are randomly displayed a couple of  dimensions</p>
      </div>
      <% @dimensions.each do |dimension| %>
        <div class="col-12 col-md-6 col-lg-4">
          <div class="card">
            <% if dimension.photo.key %>
              <%= cl_image_tag dimension.photo.key, height: 400, width: 400, crop: :fill, class: "card-img-top" %>
            <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= dimension.title %></h5>
              <p class="card-text"><%= dimension.description %></p>
              <%= link_to "Show", dimension_path(dimension), class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <div>
      <h3>HEre goes some fancy page info</h3>
    </div>
            <footer>
        <%= render "shared/footer" %>
      </footer>
    </body>
  </html>
