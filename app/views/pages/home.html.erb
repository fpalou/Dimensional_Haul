<!DOCTYPE html>
<html>
  <head>
    <title>Dimensional Haul</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="home-page">
    <%= render "shared/banner" %>
    <div class="home-info">
      <div class="row">
        <div class="col-12">
          <h3>Travel to another Dimension! Check out our parallel worlds</h3>
        </div>
  <% i = 0 %>
        <% @dimensions.each do |dimension| %>
          <div class="col-12 col-md-6 col-lg-4">
            <div class="mt-3 mr-3 card-container">
              <% if dimension.photo.key %>
              <% end %>
              <div class="home-card">
                <%= cl_image_tag dimension.photo.key, height: 400, width: 400, crop: :fill, class: "card-img-top" %>
                <h2><%= dimension.title %></h2>
                <p><%= dimension.description %></p>
                <%= link_to "Show", dimension_path(dimension), class: "btn btn-teal" %>
              </div>
            </div>
          </div>
          <% i += 1 %>
          <%break if i > 2 %>
        <% end %>
      </div>
      <%# <div class="home-info">
        <h3>Here goes some fancy page info</h3>
      </div> %>
    </div>
            <footer>
        <%= render "shared/footer" %>
      </footer>
    </body>
  </html>
